apiVersion: v1
kind: service
metadata:
  name: shopping-cart-service
  namespace: shopping-cart
spec:
  type: ClusterIP

  selector:
    app: shopping-cart

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
